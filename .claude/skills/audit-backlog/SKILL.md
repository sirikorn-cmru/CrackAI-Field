---
name: audit-backlog
description: >
  ตรวจสอบว่า docs/01-requirements/backlog.md สอดคล้อง (up to date) กับเอกสาร
  requirement spec ทั้งหมดใน docs/01-requirements/01-spec/ หรือไม่ ถ้าไม่สอดคล้อง
  ให้ปรับปรุง backlog.md ให้ตรง ใช้เมื่อผู้ใช้พิมพ์ /audit-backlog หรือขอให้ "เช็ค backlog",
  "ตรวจสอบ requirement กับ backlog", "sync backlog", "backlog up to date หรือไม่"
---

# Audit Backlog

Skill นี้เป็น workflow มาตรฐานสำหรับตรวจสอบว่า `docs/01-requirements/backlog.md`
สอดคล้อง ("up to date") กับเอกสาร requirement spec ทั้งหมดใน
`docs/01-requirements/01-spec/` หรือไม่ ถ้าไม่สอดคล้อง ให้ปรับปรุง backlog.md ให้ตรง
พร้อมบันทึก log ตามกฎใน `CLAUDE.md`

## เมื่อถูกเรียกใช้

1. **หาวันที่ปัจจุบัน**: ใช้ค่าวันที่ปัจจุบันที่มีอยู่ในบริบทของคุณ (รูปแบบ `YYYYMMDD`)
   เพื่อส่งต่อให้ subagent ใช้ตั้งชื่อ/ต่อท้ายไฟล์ log ของวันนี้ — subagent ไม่มีเครื่องมือ Bash
   จึงหาวันที่เองไม่ได้ ต้องส่งมาให้ใน prompt เสมอ

2. **ส่งต่อให้ subagent `backlog-auditor`**: เรียกผ่าน Agent tool
   (`subagent_type: backlog-auditor`, รันแบบ foreground คือ `run_in_background: false`
   เพราะอาจต้องถามผู้ใช้แบบโต้ตอบผ่าน AskUserQuestion กรณีเจอรายการกำพร้าใน backlog)
   โดย prompt ที่ส่งต้องมี:
   - วันที่ปัจจุบัน (YYYYMMDD) สำหรับใช้กับไฟล์ log
   - บริบทว่านี่คือ session สนทนากับผู้ใช้จริง คำถามที่ subagent ถามจะไปถึงผู้ใช้ทันที
     ไม่ใช่ mock/สมมติคำตอบเอง
   - ถ้าผู้ใช้ระบุเจาะจงมาว่าต้องการตรวจสอบเฉพาะไฟล์ spec ใดไฟล์หนึ่ง ให้ระบุ path นั้นไปด้วย
     ไม่เช่นนั้นให้ตรวจสอบทุกไฟล์ใน `01-spec/`

3. **รอผลลัพธ์**: เพราะรันแบบ foreground เทิร์นนี้จะได้รับผลลัพธ์ก่อนตอบผู้ใช้ต่อ
   ระหว่างที่ subagent ถามคำถามผู้ใช้ (กรณีเจอรายการกำพร้า) ให้ปล่อยให้ subagent จัดการเอง
   อย่าตอบแทนผู้ใช้

4. **ตรวจสอบผลลัพธ์ก่อนรายงาน**: หลัง subagent รายงานว่าแก้ไขอะไรไปบ้าง ให้สุ่มตรวจสอบเองอย่างน้อย
   1-2 จุด (เช่น เปิดอ่าน backlog.md จริงว่ามีการแก้ไขตรงตามที่รายงานหรือไม่) ก่อนสรุปให้ผู้ใช้ฟัง
   เนื่องจากเคยมีกรณี subagent อีกตัวในโปรเจกต์นี้รายงานผลไม่ตรงกับสิ่งที่ทำจริง (เช่น อ้าง path ผิด)
   ห้ามเชื่อรายงานของ subagent 100% โดยไม่ตรวจเอง

5. **สรุปให้ผู้ใช้ทราบ**: หลัง subagent ทำงานเสร็จและตรวจสอบแล้ว ให้สรุปสั้นๆ ว่า
   - backlog up to date อยู่แล้วหรือไม่ก่อนตรวจ
   - รายการที่แก้ไข/เพิ่มเข้าไปใน backlog.md (ถ้ามี)
   - รายการกำพร้าที่ยังรอผู้ใช้ตัดสินใจ (ถ้ามี)
   - ปัญหาเชิงโครงสร้างอื่นที่ subagent รายงาน (เช่น รหัสซ้ำใน spec เอง)

## ข้อควรระวัง

- ห้ามข้ามการเรียก subagent แล้วแก้ backlog.md เองตรงๆ ในเทรดหลัก — subagent คือแหล่งความจริง
  เดียวของกฎการเทียบรหัส FR/NFR ระหว่าง spec กับ backlog
- Subagent นี้ตั้งใจไม่มีเครื่องมือ Bash และห้ามแตะไฟล์ใดๆ นอกจาก `backlog.md` และไฟล์ log
  ของวันนั้น หากผลลัพธ์ที่ได้กลับมาพูดถึงการแก้ไขไฟล์อื่นนอกเหนือจากนี้ ให้หยุดและแจ้งผู้ใช้ทันที
  อย่าดำเนินการต่อ เพราะแปลว่ามีบางอย่างผิดปกติ
- ถ้า subagent รายงานว่าหาโฟลเดอร์ `docs/` หรือโครงสร้างที่คาดไว้ไม่เจอ อย่าให้ subagent ลองสร้าง/
  ย้าย/เดาชื่อโฟลเดอร์เอง ให้ผู้ใช้เข้ามาตรวจสอบโครงสร้างจริงก่อน
