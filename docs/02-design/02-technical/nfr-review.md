# ตรวจสอบความสอดคล้องของ NFR กับการออกแบบเชิงเทคนิค (NFR Review)

เอกสารนี้ตรวจสอบว่า NFR ทั้ง 10 ตัวใน [[backlog]] ถูกออกแบบรองรับจริงหรือไม่ในเอกสารเชิงเทคนิคทั้งหมด ([[architecture]], [[api-spec]], [[db-spec]], และไฟล์ใน `detailed-design/`) **เป็นการตรวจสอบและรายงานเท่านั้น ไม่มีการแก้ไขเอกสารเชิงเทคนิคใดๆ ในกระบวนการนี้**

อัปเดตล่าสุด: 2026-08-28 (**รอบตรวจที่ 2**) — รอบก่อนหน้า (NFR-01–09) พบ NFR-03, NFR-04, NFR-07 "รองรับบางส่วน"; รอบนี้ backlog เพิ่มเป็น **41 รายการ / NFR เพิ่มเป็น 10 ตัว** (เพิ่ม **NFR-10** จาก FR-30/FR-31 ฟีเจอร์ใหม่ "แก้ไขความขัดแย้งของข้อมูลจากการซิงค์ด้วยมือ") และเอกสารเชิงเทคนิคถูกปรับปรุงเพื่อปิดช่องว่างทั้ง 3 จุดที่รายงานไว้รอบก่อน — ตรวจซ้ำแล้วพบว่า**ปิดครบทั้ง 3 จุดจริง** (รายละเอียดในหัวข้อ "ประวัติการแก้ไขจากรอบตรวจก่อนหน้า" ด้านล่าง)

> **หมายเหตุสำคัญเกี่ยวกับวิธีตรวจ**: ไม่ได้เชื่อหัวข้อ "Mapping NFR ไปยัง Component" ใน [[architecture#5. Mapping NFR ไปยัง Component|architecture หัวข้อ 5]] เพียงอย่างเดียว — การ map ไว้ในตารางไม่ได้แปลว่ามีกลไกออกแบบจริง จึงตรวจไล่ลงไปถึง [[api-spec]]/[[db-spec]] และไฟล์ `detailed-design/` ที่เกี่ยวข้องของแต่ละ NFR ก่อนสรุปสถานะ (วิธีนี้เองที่จับ NFR-04/NFR-07 ได้ในรอบก่อน จึงใช้วิธีเดียวกันตรวจซ้ำการซ่อมในรอบนี้ ไม่เชื่อคำอธิบายการซ่อมเฉยๆ)

## สรุปผลรวม

| สถานะ | จำนวน | รหัส |
|---|---|---|
| รองรับแล้ว | 10 | NFR-01, NFR-02, NFR-03, NFR-04, NFR-05, NFR-06, NFR-07, NFR-08, NFR-09, NFR-10 |
| รองรับบางส่วน | 0 | — |
| ยังไม่รองรับ | 0 | — |

## ตารางตรวจสอบรายตัว

| รหัส | คำอธิบายสั้น | สถานะ | เอกสาร/ส่วนที่พบ | สิ่งที่ยังขาด | แนะนำให้รันอะไรต่อ |
|---|---|---|---|---|---|
| NFR-01 | Availability / Offline Support | รองรับแล้ว | [[architecture#2. Logical Component\|architecture 2. Field Client App]], [[architecture#4. Data Flow Diagram ต่อ Journey หลัก\|architecture 4.1]], [[api-spec#4. บันทึกข้อมูลอาคารและสภาพแวดล้อม\|api-spec 4.x]], [[api-spec#7. ถ่ายภาพและประเมินรอยร้าวด้วย AI พร้อมทางเลือกกรอกเอง (Human-in-the-loop)\|api-spec 7.2]], [[detailed-design/authentication-login\|authentication-login 1.3]], [[detailed-design/ai-crack-photo-analysis\|ai-crack-photo-analysis]] | ไม่มี | — |
| NFR-02 | Performance / Reliability (ซิงค์ไฟล์ภาพขนาดใหญ่) | รองรับแล้ว | [[api-spec#14. การทำงานออฟไลน์และซิงค์ข้อมูลภาคสนาม\|api-spec 14.1]], [[db-spec#6.1 DamagePhoto\|db-spec 6.1 DamagePhoto]], [[db-spec#6.3 Sketch\|db-spec 6.3 Sketch]] (`sync_status`), [[detailed-design/offline-sync\|offline-sync]] (sequence + state diagram + edge case "ไฟล์ภาพส่งไม่สำเร็จบางส่วน") | รูปแบบ chunk/resume จริงยังไม่ตัดสินใจ (รอ `technology-stack.md` — ระบุไว้แล้วเป็นประเด็นรอตัดสินใจ ไม่ใช่ช่องว่างเชิง logical) | — |
| NFR-03 | Data Integrity (จัดการความขัดแย้งของข้อมูล) | **รองรับแล้ว** (เดิม: รองรับบางส่วน) | [[db-spec#4.1 SurveyedBuilding\|db-spec 4.1]] (`data_version`, `last_modified_at`), [[db-spec#8.1 SyncConflict\|db-spec 8.1 SyncConflict]], [[db-spec#8.2 SyncConflictVersion\|db-spec 8.2 SyncConflictVersion]], [[db-spec#10. กฎทางธุรกิจที่กระทบโครงสร้างข้อมูล\|db-spec หัวข้อ 10 ข้อ 2-3]] (เหตุผลจำกัดขอบเขตของ `client_generated_id` เพิ่มใน entity ย่อย 6 ตัว), [[api-spec#14.2 ตรวจสอบ/แก้ไขความขัดแย้งของข้อมูลที่ซิงค์\|api-spec 14.2]], [[api-spec#15. การแก้ไขความขัดแย้งของข้อมูลจากการซิงค์ (Manual Conflict Resolution)\|api-spec 15.1, 15.2]], [[detailed-design/offline-sync\|offline-sync]], [[detailed-design/manual-conflict-resolution\|manual-conflict-resolution]] | ไม่มี (ทั้งสองช่องว่างเดิมปิดแล้ว — ดูรายละเอียดการประเมินใน "ประวัติการแก้ไขจากรอบตรวจก่อนหน้า") | — |
| NFR-04 | Usability / Compatibility (ใช้งานได้ดีบนมือถือ/แท็บเล็ตกลางแจ้ง) | **รองรับแล้ว** (เดิม: รองรับบางส่วน) | [[architecture#5. Mapping NFR ไปยัง Component\|architecture หัวข้อ 5]], [[detailed-design/building-environment-info#5. Edge Case เพิ่มเติมสำหรับการใช้งานกลางแจ้งในพื้นที่ภัยพิบัติ (NFR-04)\|building-environment-info หัวข้อ 5]] (ข้อมูลหายเมื่อแอปปิดกลางฟอร์ม, ถุงมือ/มือเปียก, อ่านหน้าจอกลางแดด, หลายขนาดหน้าจอ), [[detailed-design/ai-crack-photo-analysis#5. Edge Case เพิ่มเติมสำหรับการใช้งานกลางแจ้งในพื้นที่ภัยพิบัติ (NFR-04)\|ai-crack-photo-analysis หัวข้อ 5]], [[detailed-design/offline-sync#6. Edge Case และวิธีจัดการ\|offline-sync หัวข้อ 6]] (แอปดับกลางคันระหว่างมีระเบียนค้างในคิว) | ไม่มี (ยังไม่ได้ขยาย edge case ชุดเดียวกันไปยังไฟล์ field-facing ที่เหลือ เช่น `structural-damage-assessment.md`, `surveyor-info-duration.md`, `additional-sketch.md` — แต่ประเมินว่าไม่บล็อก เพราะ 3 ไฟล์ที่มีแล้วครอบคลุมหมวดปัญหาหลักครบ [data loss/touch-glare/crash-resilience] และ `offline-sync.md` เป็น single source ของ crash/queue resilience ที่ใช้ร่วมกันทุกฟีเจอร์อยู่แล้ว) | เสริมได้ (ไม่บังคับ): `sync-detailed-design` หากต้องการขยาย edge case ชุดเดิมไปยังไฟล์ field-facing ที่เหลือเพื่อความสม่ำเสมอ |
| NFR-05 | Auditability / Accountability | รองรับแล้ว | [[db-spec#7.2 AuditTrailEntry\|db-spec 7.2 AuditTrailEntry]], [[api-spec#7.3 ยืนยัน/แก้ไขผลวิเคราะห์ AI ก่อนบันทึกจริง\|api-spec 7.3]], [[api-spec#10.2 ลงลายมือชื่อดิจิทัลรับรองผล\|api-spec 10.2]] (เลขข้อเดิม เลื่อนเป็น 10.3 ในรอบนี้), [[api-spec#15.2 เปรียบเทียบเวอร์ชันข้อมูลที่ขัดแย้งกันและเลือก/รวมค่าด้วยมือ\|api-spec 15.2]] (ขยายให้ครอบคลุมการแก้ไขความขัดแย้งด้วย), [[detailed-design/ai-crack-photo-analysis\|ai-crack-photo-analysis]], [[detailed-design/survey-review-signature\|survey-review-signature]], [[detailed-design/manual-conflict-resolution\|manual-conflict-resolution]] | ไม่มี | — |
| NFR-06 | Accuracy (ความแม่นยำ GPS และคุณภาพภาพ) | รองรับแล้ว | [[db-spec#4.1 SurveyedBuilding\|db-spec 4.1]] (`gps_accuracy_meters`), [[db-spec#6.1 DamagePhoto\|db-spec 6.1]] (`image_quality_check_status`), [[api-spec#4.1 เริ่มต้นแบบสำรวจอาคารใหม่\|api-spec 4.1]], [[api-spec#7.1 ถ่ายภาพประกอบความเสียหาย/ผูกกับบริเวณ\|api-spec 7.1]], [[detailed-design/ai-crack-photo-analysis\|ai-crack-photo-analysis]] (ขั้นตอนตรวจสอบคุณภาพภาพ + edge case) | ค่าตัวเลขเกณฑ์ยอมรับได้จริง (ทศนิยม GPS, ค่าความคมชัดขั้นต่ำ) ยังไม่กำหนด — ระบุไว้แล้วว่าเป็นประเด็นรอ `technology-stack.md` ใน [[api-spec#16. ประเด็นรอตัดสินใจ\|api-spec หัวข้อ 16]] ไม่ใช่ช่องว่างของการออกแบบ | — |
| NFR-07 | Scalability (รองรับหลายทีมซิงค์พร้อมกันจำนวนมาก) | **รองรับแล้ว** (เดิม: รองรับบางส่วน) | [[architecture#2. Logical Component\|architecture หัวข้อ 2/3]] — เพิ่ม sub-component **"คิวรับคำขอซิงค์ฝั่งเซิร์ฟเวอร์ (Server-side Sync Intake Queue)"** (node `INTAKE`) แยกจาก "คิวรอซิงค์ในเครื่อง" ฝั่ง Field Client อย่างชัดเจน ปรากฏใน component diagram + data flow diagram 4.1 ("ผ่านคิวรับคำขอฝั่งเซิร์ฟเวอร์"), [[architecture#5. Mapping NFR ไปยัง Component\|architecture หัวข้อ 5]] (แนวทางเชิงหลักการขยายเป็นย่อหน้าเต็ม), [[api-spec#14.1 ซิงค์ข้อมูลแบบสำรวจที่ค้างจากอุปกรณ์\|api-spec 14.1]] (ผูก business rule กับ component นี้ตรงๆ) | ไม่มี (กลไก logical มีแล้ว) — อัลกอริทึม/ตัวเลข rate-limit จริงยังไม่กำหนด แต่ระบุไว้แล้วว่าเป็นประเด็นรอ `technology-stack.md` ([[api-spec#16. ประเด็นรอตัดสินใจ\|api-spec หัวข้อ 16]]) ไม่ใช่ช่องว่างของการออกแบบระดับ logical อีกต่อไป | — |
| NFR-08 | Security (RBAC — เข้าถึงเฉพาะผู้มีสิทธิ์ตามบทบาท) | รองรับแล้ว | [[architecture#4. Data Flow Diagram ต่อ Journey หลัก\|architecture 4.3]] ("ตรวจสอบสิทธิ์ตามบทบาทก่อนดำเนินการ"), [[db-spec#2.1 User\|db-spec 2.1 User.role]], [[api-spec#2. จัดการผู้ใช้และสิทธิ์การเข้าถึง\|api-spec หัวข้อ 2]] (ทุก operation ระบุ "ผู้เรียกได้"), [[detailed-design/user-management\|user-management]], [[detailed-design/authentication-login\|authentication-login]] | กลไก RBAC จริง (ตาราง permission แยก หรือฝังในตัว `role`) ยังไม่ตัดสินใจ — ระบุไว้แล้วเป็นประเด็นรอ `technology-stack.md` ใน [[db-spec#11. ประเด็นรอตัดสินใจ\|db-spec หัวข้อ 11]] ไม่ใช่ช่องว่างของการออกแบบระดับ logical | — |
| NFR-09 | Security (อายุ session ที่แคชไว้ขณะออฟไลน์มีจำกัด) | รองรับแล้ว | [[db-spec#2.2 Session\|db-spec 2.2 Session]] (`expires_at`, `is_offline_cached`, `last_verified_online_at`), [[api-spec#1.3 เข้าสู่ระบบด้วย credential ที่แคชไว้ขณะออฟไลน์\|api-spec 1.3]], [[api-spec#1.4 ต่ออายุ/ยืนยันตัวตนใหม่เมื่อกลับมามีสัญญาณ\|api-spec 1.4]], [[detailed-design/authentication-login\|authentication-login]] (sequence + state diagram ครบ) | ไม่มี | — |
| NFR-10 | Data Integrity (Reporting Consistency) — ห้ามระเบียนที่ยังขัดแย้งค้างอยู่ไหลเข้า dashboard/รายงาน | **รองรับแล้ว** (ตัวใหม่รอบนี้) | [[architecture#3. Component Diagram\|architecture 3]] (edge "กรองระเบียนขัดแย้งออกจาก dashboard/รายงาน"), [[architecture#5. Mapping NFR ไปยัง Component\|architecture หัวข้อ 5]], [[db-spec#8.2 SyncConflictVersion\|db-spec 8.2]] (กฎ: `sync_status` ต้องเปลี่ยนเป็น "ซิงค์สำเร็จ" ในธุรกรรมเดียวกับการปลดสถานะ `SyncConflict`), [[api-spec#11.1 ดู Dashboard ภาพรวมผลสำรวจ\|api-spec 11.1]], [[api-spec#12.1 ส่งออกรายงานผลการสำรวจ\|api-spec 12.1]], [[api-spec#13.1 ค้นหา/กรองรายการอาคารที่สำรวจแล้ว\|api-spec 13.1]] (ทั้ง 3 operation มีกฎกรอง `sync_status = มีความขัดแย้งรอแก้ไข` ออกชัดเจน), [[detailed-design/dashboard-overview\|dashboard-overview]], [[detailed-design/export-report\|export-report]], [[detailed-design/search-filter-buildings\|search-filter-buildings]] (ทั้ง 3 ไฟล์มี edge case ระบุการกรองออกตรงๆ), [[detailed-design/manual-conflict-resolution\|manual-conflict-resolution]] (จุดปลดเงื่อนไข) | ไม่มี | — |

## ประวัติการแก้ไขจากรอบตรวจก่อนหน้า (ตรวจซ้ำแล้ว ยืนยันว่าปิดจริง)

### NFR-03 — Data Integrity (เดิม: รองรับบางส่วน → ตอนนี้: รองรับแล้ว)

- **ช่องว่าง (1) กลไกแก้ไขความขัดแย้งด้วยมือ — ปิดแล้ว**: เพิ่ม FR-30/FR-31 ([[feature-list#15. แก้ไขความขัดแย้งของข้อมูลจากการซิงค์ด้วยมือ (Manual Conflict Resolution)|ฟีเจอร์ 15]]) + entity [[db-spec#8.1 SyncConflict|SyncConflict]]/[[db-spec#8.2 SyncConflictVersion|SyncConflictVersion]] (เก็บทุกเวอร์ชันที่ขัดแย้งไว้เปรียบเทียบ) + operation [[api-spec#15.1 แสดงรายการระเบียนที่มีความขัดแย้งของข้อมูลรอการแก้ไขด้วยมือ|15.1]]/[[api-spec#15.2 เปรียบเทียบเวอร์ชันข้อมูลที่ขัดแย้งกันและเลือก/รวมค่าด้วยมือ|15.2]] + ไฟล์ [[detailed-design/manual-conflict-resolution|manual-conflict-resolution.md]] ที่มี sequence diagram, state diagram (`SyncConflict.status`), ตาราง edge case ครบ — ตรวจแล้วว่าไม่ใช่แค่ประกาศแนวคิดแต่มีกลไกจริงครบทั้ง 3 ชั้นเอกสาร ยืนยันปิดช่องว่างนี้จริง
- **ช่องว่าง (2) `client_generated_id` ของ entity ย่อย — ปิดแล้ว พร้อมประเมินขอบเขตที่จำกัดไว้ว่าเพียงพอ**: [[db-spec]] เพิ่ม `client_generated_id` ให้ entity ย่อย 6 ตัว (`SurveyParticipant`, `SurroundingHazard`, `ExternalDamage`, `StructuralDamage`, `ComponentDamage`, `ElectricalSystemDamage`) แต่จงใจจำกัดขอบเขตไว้ว่าใช้เพื่อ **idempotent upsert + referential stability เท่านั้น** ส่วนการตรวจจับความขัดแย้ง (conflict detection) ยังอยู่ที่ระดับ `SurveyedBuilding` (aggregate) จุดเดียวตามเดิม ([[api-spec#14.1 ซิงค์ข้อมูลแบบสำรวจที่ค้างจากอุปกรณ์|api-spec 14.1]], [[db-spec#10. กฎทางธุรกิจที่กระทบโครงสร้างข้อมูล|db-spec หัวข้อ 10 ข้อ 2-3]])
  - **ประเมินว่าเหตุผลนี้เพียงพอหรือไม่**: **เพียงพอ** เพราะ (ก) ตาม [[user-journey]] ทีมสำรวจกรอกฟอร์มทั้งชุดของอาคารหนึ่งบนอุปกรณ์เดียวในช่วงเวลาเดียวกันเสมอ ความขัดแย้งที่เกิดขึ้นจริงในทางปฏิบัติจึงมักเป็นระดับ "อาคารทั้งอาคารถูกสำรวจซ้ำจากคนละอุปกรณ์" ไม่ใช่ "field ย่อยของอาคารเดียวกันถูกแก้จากอุปกรณ์ต่างกันในช่วงเวลาต่างกัน", (ข) การแก้ไข entity ย่อยใดๆ ต้องกระทบ `SurveyedBuilding.data_version`/`last_modified_at` ของอาคารแม่เสมอ (เป็น aggregate) ทำให้ความขัดแย้งระดับ field ย่อยจะถูกยกระดับเป็นความขัดแย้งของอาคารแม่โดยอัตโนมัติอยู่แล้ว ไม่มีทางหลุดรอดไปไม่ถูกตรวจจับ, (ค) `SyncConflictVersion.snapshot_content` เก็บค่าทั้งอาคารรวม entity ย่อย ณ เวลานั้นไว้เปรียบเทียบครบอยู่แล้ว จึงไม่จำเป็นต้องมี conflict detection แยกระดับ field — สรุป: การจำกัดขอบเขตนี้เป็นการออกแบบที่สมเหตุสมผล ไม่ใช่ช่องว่างที่หลงเหลือ

### NFR-04 — Usability / Compatibility (เดิม: รองรับบางส่วน → ตอนนี้: รองรับแล้ว)

รอบก่อนพบว่า "map ไว้ในตารางแต่ไม่มีการออกแบบจริงรองรับ" — รอบนี้เพิ่มตาราง Edge Case เชิง logical ใน 3 ไฟล์: [[detailed-design/building-environment-info#5. Edge Case เพิ่มเติมสำหรับการใช้งานกลางแจ้งในพื้นที่ภัยพิบัติ (NFR-04)|building-environment-info หัวข้อ 5]] (ข้อมูลหายเมื่อแอปปิดกลางฟอร์มยาว → ต้องบันทึกทันทีที่กรอกเสร็จแต่ละ field/หมวดย่อย, สวมถุงมือ/มือเปียกกดพลาด → ตัวเลือกต้องมีขนาดสัมผัสใหญ่พอ, อ่านหน้าจอกลางแดดจ้า → ข้อความแจ้งเตือนต้องคมชัด/คอนทราสต์สูงไม่พึ่งสีอย่างเดียว, ขนาดหน้าจอต่างกัน → ฟอร์มต้องแสดงผล/เลื่อนดูได้ครบ), [[detailed-design/ai-crack-photo-analysis#5. Edge Case เพิ่มเติมสำหรับการใช้งานกลางแจ้งในพื้นที่ภัยพิบัติ (NFR-04)|ai-crack-photo-analysis หัวข้อ 5]], [[detailed-design/offline-sync#6. Edge Case และวิธีจัดการ|offline-sync หัวข้อ 6]] (แอปดับกลางคันระหว่างมีระเบียนค้างในคิว → ระเบียนต้องไม่หาย คิวต้องทำต่อได้)
- **ประเมินว่าครอบคลุมพอหรือยัง**: **พอแล้วสำหรับสถานะ "รองรับแล้ว"** เหตุผล: 3 ไฟล์นี้ครอบคลุมหมวดปัญหาหลักของ NFR-04 ครบ (การสูญเสียข้อมูล, การโต้ตอบด้วยถุงมือ/แสงจ้า, ความทนทานต่อการขัดจังหวะของการซิงค์) และ [[detailed-design/offline-sync|offline-sync.md]] ถูกออกแบบไว้แต่แรกให้เป็น "แหล่งอ้างอิงหลัก (single source) ของ lifecycle `sync_status`" ที่ใช้ร่วมกันทุกฟีเจอร์ที่สร้างข้อมูลออฟไลน์อยู่แล้ว เอดจ์เคสเรื่อง crash-resilience จึงมีผลครอบคลุมฟีเจอร์อื่นโดยอ้อมโดยไม่ต้องเขียนซ้ำทุกไฟล์ — ไม่ถือเป็นช่องว่างที่บล็อกสถานะ "รองรับแล้ว" แต่การขยายชุด edge case เดียวกัน (โดยเฉพาะเรื่องถุงมือ/แสงจ้าที่เฉพาะเจาะจงกับแต่ละหน้าจอ) ไปยังไฟล์ field-facing ที่เหลือ (`structural-damage-assessment.md`, `surveyor-info-duration.md`, `additional-sketch.md`) จะทำให้เอกสารสม่ำเสมอมากขึ้น — เป็นข้อเสนอแนะเสริมไม่บังคับ

### NFR-07 — Scalability (เดิม: รองรับบางส่วน → ตอนนี้: รองรับแล้ว)

รอบก่อนพบว่าไม่มี component/กลไกคิวฝั่งเซิร์ฟเวอร์เลย มีแต่ "คิวรอซิงค์ในเครื่อง" ฝั่ง Field Client ซึ่งแก้คนละปัญหา — รอบนี้ [[architecture]] เพิ่ม sub-component ใหม่ **"คิวรับคำขอซิงค์ฝั่งเซิร์ฟเวอร์ (Server-side Sync Intake Queue)"** เป็น node `INTAKE` แยกต่างหากภายใน Sync & Conflict Resolution Service ปรากฏจริงทั้งใน component diagram ([[architecture#3. Component Diagram|architecture หัวข้อ 3]]: `FCQueue -->|ซิงค์เมื่อมีสัญญาณ| INTAKE` และ `INTAKE -->|จัดคิว/จำกัดอัตราตามโหลด (NFR-07)| SYNC`), data flow diagram 4.1 ("ส่งข้อมูล/ไฟล์ภาพที่ค้างอยู่ ผ่านคิวรับคำขอฝั่งเซิร์ฟเวอร์"), ตาราง NFR mapping (ขยายเป็นคำอธิบายเต็มย่อหน้าอธิบายรูปแบบโหลดที่กระจุกตัวเป็นช่วง), และ [[api-spec#14.1 ซิงค์ข้อมูลแบบสำรวจที่ค้างจากอุปกรณ์|api-spec 14.1]] ผูก business rule กับ component นี้ตรงๆ
- **ประเมินว่าขยับขึ้นครบหรือไม่**: **ครบ** — ตอนนี้มีกลไกระดับ logical (component แยกต่างหากที่ทำหน้าที่จัดคิว/จำกัดอัตรา) ไม่ใช่แค่ประโยคลอยๆ อีกต่อไป ส่วนอัลกอริทึม/ตัวเลข rate-limit ที่เป็นรูปธรรมยังคงเป็นประเด็นรอ `technology-stack.md` เหมือนกับ NFR-06 ที่เกณฑ์ตัวเลขรอตัดสินใจแต่กลไกออกแบบไว้ครบแล้ว — จัดอยู่ในหมวดเดียวกัน ไม่ใช่ช่องว่างของการออกแบบอีกต่อไป

## NFR-10 — Data Integrity (Reporting Consistency) — ตัวใหม่ของรอบนี้

ตรวจสอบแล้วว่ามีการออกแบบรองรับครบทั้ง 3 ชั้น ไม่ใช่แค่ map ไว้ลอยๆ: [[architecture]] มีทั้ง edge ใน component diagram และคำอธิบายเต็มในตาราง NFR mapping, [[db-spec#8.2 SyncConflictVersion|db-spec]] มีกฎทางธุรกิจระบุชัดว่าการปลดสถานะขัดแย้งกับการเปลี่ยน `sync_status` เป็น "ซิงค์สำเร็จ" ต้องเกิดในธุรกรรมเดียวกัน, [[api-spec]] มี 3 operation ที่เกี่ยวข้องกับการอ่านข้อมูลสรุป (11.1 Dashboard, 12.1 ส่งออกรายงาน, 13.1 ค้นหา/กรอง) ทุกตัวมีกฎกรอง `sync_status = มีความขัดแย้งรอแก้ไข` ออกชัดเจนเป็นรายบรรทัด ไม่ใช่แปะรหัสลอยๆ แบบที่เคยพบใน NFR-04/07 รอบก่อน และ detailed-design ทั้ง 4 ไฟล์ที่เกี่ยวข้อง ([[detailed-design/dashboard-overview|dashboard-overview]], [[detailed-design/export-report|export-report]], [[detailed-design/search-filter-buildings|search-filter-buildings]], [[detailed-design/manual-conflict-resolution|manual-conflict-resolution]]) มีตาราง edge case ระบุการกรองออก/จุดปลดเงื่อนไขตรงกันสอดคล้องกันทั้งหมด — **ไม่พบช่องว่าง**

## เอกสารที่เกี่ยวข้อง

- [[backlog]] — แหล่งความจริงของรายการ NFR ทั้ง 10 ตัว (เพิ่ม NFR-10 จาก FR-30/FR-31 ในรอบนี้)
- [[architecture]], [[api-spec]], [[db-spec]] — เอกสารเชิงเทคนิคหลักที่ใช้ตรวจสอบ
- [[detailed-design/offline-sync]], [[detailed-design/authentication-login]], [[detailed-design/ai-crack-photo-analysis]], [[detailed-design/survey-review-signature]], [[detailed-design/user-management]], [[detailed-design/manual-conflict-resolution]], [[detailed-design/building-environment-info]], [[detailed-design/dashboard-overview]], [[detailed-design/export-report]], [[detailed-design/search-filter-buildings]] — ไฟล์ detailed-design ที่ตรวจละเอียดสำหรับ NFR ที่เกี่ยวข้องโดยตรง
- [[feature-list]], [[user-journey]] — บริบทฟีเจอร์/การใช้งานที่ NFR แต่ละตัวต้องรองรับ (รวมฟีเจอร์ 15 ใหม่)
