# Prototype v1 — CrackAI-Field

Clickable HTML Prototype เวอร์ชันแรกของโปรเจกต์ แปลงจาก [[feature-list]] + [[user-journey]] โดยยึด [[DESIGN.md]] เป็น single source of truth ของภาพทั้งหมด (สี ตัวอักษร ระยะห่าง องค์ประกอบ UI) อ้างอิงต้นทาง [[20260828-01-flood-damage-survey]] และ [[backlog]]

โฟลเดอร์: `docs/02-design/01-prototypes/20260830-01-v1/`

## ขอบเขต (Scope)

**สร้างเฉพาะฟีเจอร์ระดับ Must have ทั้ง 11 รายการ** จากทั้งหมด 15 ฟีเจอร์ใน [[feature-list]] ตามที่ยืนยันไว้ก่อนเริ่มงาน

**ไม่ได้สร้างในรอบนี้ (Should have — 4 รายการ):**

| ฟีเจอร์ | รหัส | เหตุผลที่ไม่รวม |
|---|---|---|
| 5. วาดภาพประกอบเพิ่มเติม | FR-15 | Should have — เลื่อนออกจาก scope รอบนี้ตามที่ยืนยัน |
| 8. มอบหมายงานสำรวจให้ทีม | FR-20 | Should have — journey ของผู้สำรวจภาคสนามในต้นแบบนี้จึงเริ่มจากหน้าสำรวจโดยตรง ไม่มีหน้าจอสร้าง/มอบหมายงานของหัวหน้าผู้สำรวจ |
| 11. ส่งออกรายงานผลการสำรวจ | FR-23 | Should have — หน้า Dashboard มีหมายเหตุระบุไว้ชัดเจนว่ายังไม่มีปุ่มส่งออก |
| 12. ค้นหา/กรองรายการอาคารที่สำรวจแล้ว | FR-24 | Should have — ตารางรายการทุกหน้า (lead-review-list, dashboard) เป็นรายการนิ่ง ไม่มีช่องค้นหา/ตัวกรอง |

ผลที่ตามมาจากการตัด feature 8: journey ของผู้สำรวจภาคสนามในต้นแบบนี้ตัดขั้นตอน "รับมอบหมายงานจากหัวหน้าผู้สำรวจ" ออก และเริ่มจากหน้าข้อมูลอาคารโดยตรงหลังเข้าสู่ระบบ (สมมติว่าอาคารที่จะสำรวจถูกกำหนดไว้แล้ว)

## ตารางไฟล์ ↔ Journey ↔ FR/NFR

| ไฟล์ | Journey / บทบาท | ฟีเจอร์ | รหัส FR/NFR |
|---|---|---|---|
| `style.css` | ทุก journey | — | แปลง token จาก DESIGN.md §2–§11 |
| `login.html` | ทุกบทบาท | 14. ยืนยันตัวตนและเข้าสู่ระบบ | FR-25, FR-26, FR-27, NFR-09 |
| `field-survey-building.html` | ผู้สำรวจภาคสนาม | 1. บันทึกข้อมูลอาคารและสภาพแวดล้อม | FR-01, FR-02, FR-03, FR-04, NFR-06 |
| `field-survey-damage.html` | ผู้สำรวจภาคสนาม | 2. ประเมินความเสียหายโครงสร้างและส่วนประกอบอาคาร | FR-05, FR-06, FR-07, FR-08, FR-09, FR-11 |
| `field-survey-photo-ai.html` | ผู้สำรวจภาคสนาม | 4. ถ่ายภาพและประเมินรอยร้าวด้วย AI (human-in-the-loop) | FR-12, FR-13, FR-14, FR-28, FR-29, NFR-05 |
| `field-survey-summary.html` | ผู้สำรวจภาคสนาม | 3. สรุปผลประเมินเป็น 3 ระดับสี · 6. ข้อมูลผู้สำรวจและระยะเวลา | FR-10, FR-16, FR-17 |
| `field-sync-status.html` | ผู้สำรวจภาคสนาม | 13. การทำงานออฟไลน์และซิงค์ข้อมูลภาคสนาม | NFR-01, NFR-02, NFR-03, NFR-04, NFR-07 |
| `lead-review-list.html` | หัวหน้าผู้สำรวจ | 7. ตรวจทานและรับรองผลสำรวจ (รายการ) | FR-19 |
| `lead-review-detail.html` | หัวหน้าผู้สำรวจ | 7. ตรวจทานและรับรองผลสำรวจด้วยลายเซ็นดิจิทัล | FR-18, FR-19 |
| `admin-users.html` | ผู้ดูแลระบบ | 9. จัดการผู้ใช้และสิทธิ์การเข้าถึง | FR-21, NFR-08 |
| `dashboard.html` | หน่วยงานส่วนกลาง/ผู้บริหาร | 10. Dashboard ภาพรวมผลสำรวจ | FR-22 |
| `conflict-list.html` | หัวหน้าผู้สำรวจ, ผู้ดูแลระบบ | 15. แก้ไขความขัดแย้งของข้อมูลด้วยมือ (รายการ) | FR-30 |
| `conflict-detail.html` | หัวหน้าผู้สำรวจ, ผู้ดูแลระบบ | 15. แก้ไขความขัดแย้งของข้อมูลด้วยมือ (เปรียบเทียบ/แก้ไข) | FR-31, NFR-10 |
| `index.html` | ทุก journey | หน้ารวมลิงก์ จัดกลุ่มตาม journey พร้อม badge บทบาท | — |

รวม **13 หน้าจอ HTML + 1 stylesheet** ครอบคลุมรหัส FR-01–14, FR-16–19, FR-21, FR-22, FR-25–31 และ NFR-01–10 (ยกเว้น NFR ที่ผูกกับฟีเจอร์ Should have) — FR-15, FR-20, FR-23, FR-24 ไม่ปรากฏในต้นแบบนี้ตามขอบเขตที่ตกลง

## จุดที่เน้นตาม NFR/หลักการออกแบบ (ตรวจสอบตัวเองแล้ว)

- **ตัวเลือกระดับความเสียหาย 3 สี** (`field-survey-summary.html`, `lead-review-detail.html`) — ไอคอนรูปทรงต่างกัน (วงกลม/สามเหลี่ยม/แปดเหลี่ยม) + ข้อความไทย + สีครบ ไม่มีตัวเลือกใดถูกเลือกไว้ล่วงหน้า (UX-02, DESIGN.md §3.4)
- **การ์ดผลวิเคราะห์ AI** (`field-survey-photo-ai.html`) — ขอบประ + ป้าย "AI เสนอ — รอการยืนยัน" + ปุ่มสามทาง (ยืนยัน/แก้ไข/กรอกเอง) ทุกปุ่มใช้คลาส `.btn-secondary`/`.btn-primary` ขนาดเท่ากันทั้งหมด (`.btn-field` height 56px เต็มความกว้าง) ตามข้อกำหนด UX-08 ว่ากรอกเองต้องมีน้ำหนักเท่าเส้นทาง AI — มีปุ่มจำลองสถานการณ์ (JS แบบ vanilla ไม่พึ่ง CDN) ให้กดดูได้ทั้งเส้นทาง AI สำเร็จ/ไม่สำเร็จ/กรอกเองตั้งแต่ต้น
- **ตัวบ่งชี้สถานะซิงค์** ปรากฏ sticky อยู่บนทุกหน้าจอภาคสนาม (`field-survey-*.html`) ใช้ `.sync-indicator.offline` ซึ่งใช้โทน `--state-offline-*` (ไม่ใช่สีแดง/ไอคอนเตือนภัย) ตาม UX-07
- **พื้นที่กด** — ปุ่ม/ช่องกรอกบนหน้าจอภาคสนามทั้งหมดใช้คลาส `.btn-field`/`.input` สูง `--touch-field` (56px) ตัวเลือกระดับความเสียหายและปุ่มยืนยันผล AI ใช้ `--touch-critical` (64px) ผ่านคลาส `.severity-option`/`.ai-card .btn`
- **ตัวอักษรภาคสนาม** — `body` ใช้ `--text-base` 17px / `--leading-normal` 1.70 เป็นค่าเริ่มต้นของ `:root` หน้าจอ back-office (`.layout-office`) override เป็นสเกลเดสก์ท็อปตาม DESIGN.md §2.5
- **Dashboard** (`dashboard.html`) ใช้เฉพาะสีสถานะความเสียหาย (เขียว/เหลือง/แดง) ที่ผูกความหมายตายตัวอยู่แล้วใน DESIGN.md §2.3 เป็นสีของ stacked bar (ไม่ใช่จานสี categorical ใหม่) มี legend + ตารางข้อมูลสำรองสำหรับการเข้าถึง (ปรึกษา skill `dataviz` ก่อนออกแบบหน้านี้)
- **Manual Conflict Resolution** (`conflict-list.html`, `conflict-detail.html`) จำกัดการเข้าถึงไว้เฉพาะหัวหน้าผู้สำรวจ/ผู้ดูแลระบบตาม spec — ไม่มีลิงก์เข้าถึงจากหน้าจอผู้สำรวจภาคสนามหรือหน่วยงานส่วนกลาง ใช้ two-column comparison + `--font-mono` สำหรับค่าที่ต้องเทียบตัวต่อตัว + ไฮไลต์เฉพาะ field ที่ต่างกันด้วย `--state-conflict-bg` ตาม DESIGN.md §3.7
- **การกระทำที่ย้อนกลับไม่ได้** (ส่งผลสำรวจ, ลงลายเซ็น, ปลดสถานะขัดแย้ง) ทุกจุดมี dialog สรุปผลลัพธ์ก่อนยืนยันตาม UX-09
- **การปิดการใช้งานบัญชี** (`admin-users.html`) **ย้อนกลับได้** โดยเปิดใช้งานบัญชีคืนภายหลัง (ตรงกับ operation 2.4 "เปิดใช้งานบัญชีผู้ใช้คืน" ใน [[api-spec#2.4 เปิดใช้งานบัญชีผู้ใช้คืน|api-spec.md]]) — dialog ยืนยันจึงยังคงเป็น `.btn-destructive` (เพราะกระทบสิทธิ์การเข้าถึงทันที) แต่ไม่นับรวมอยู่ในหมวดย้อนกลับไม่ได้ข้างต้น (แก้ไขจากการตรวจพบโดย `/audit-prototype` ว่าข้อความ dialog เดิม "ย้อนกลับได้โดยเปิดใช้งานใหม่ภายหลัง" ขัดกับการจัดหมวดในเอกสารนี้ก่อนหน้า)
- **negative case ของการเข้าสู่ระบบ** (`login.html`) เพิ่มปุ่มสาธิต 3 สถานการณ์ตามรูปแบบเดียวกับ `field-survey-photo-ai.html` (toggle panel ด้วย vanilla JS): ข้อมูลเข้าสู่ระบบไม่ถูกต้อง (FR-25 AC-2 · TC-14-02, ใช้ `.field-error`/`.input.error`), ไม่มีสิทธิ์เข้าถึงหน้าจอนอกบทบาท (FR-26 AC-2 · TC-14-05, ใช้ `.banner.danger` — ส่วนขยายใหม่ของ `.banner` ใน `style.css` ประกอบจากโทเค็น `--severity-danger-*` เดิม ตามรูปแบบเดียวกับ `.banner.offline`/`.conflict`/`.info` ที่มีอยู่แล้ว), credential ที่แคชไว้ออฟไลน์หมดอายุ (NFR-09 AC-1 · TC-14-07/08, ใช้ `.banner.danger` เช่นกัน)
- **empty state ของ Dashboard** (`dashboard.html`) เพิ่มการ์ดสาธิตพื้นที่ที่ยังไม่มีอาคารถูกสำรวจ (FR-22 AC-2) ใช้คลาส `.empty-state` เดิมใน `style.css` ตรงตามกติกา DESIGN.md §3.9 ที่ต้องอธิบายทั้งสาเหตุและสิ่งที่ควรทำต่อ ไม่ใช่แค่คำว่า "ไม่มีข้อมูล" — เป็นการ์ดสาธิตแยกต่างหาก ไม่ใช่การกรองข้อมูลจริง เพราะระบบค้นหา/กรอง (ฟีเจอร์ 12) ยังไม่อยู่ในขอบเขต

## ข้อจำกัดที่ทราบของต้นแบบนี้ (ยังไม่ได้ตรวจด้วยเบราว์เซอร์จริง)

- ตรวจสอบด้วยการอ่านไฟล์กลับ (โครงสร้าง, `href` ระหว่างหน้า, การอ้างอิง `style.css`) เท่านั้น ยังไม่ได้เปิดดูจริงในเบราว์เซอร์ — เป็น follow-up ที่ควรทำในเทรดหลัก
- ฟอนต์ IBM Plex Sans Thai/IBM Plex Mono โหลดผ่าน Google Fonts `@import` ใน `style.css` (ต้องมีอินเทอร์เน็ตตอนเปิดดูจึงจะเห็นฟอนต์ตามที่ตั้งใจ ถ้าออฟไลน์จะ fallback เป็น system font ที่ระบุไว้ใน font stack)
- ไอคอนในต้นแบบวาดด้วย inline SVG พื้นฐาน (วงกลม/สามเหลี่ยม/แปดเหลี่ยม) ยังไม่ใช่ชุดไอคอนจริง เนื่องจาก DESIGN.md §5 ระบุว่า "ชุดไอคอนใด" ยังไม่ตัดสินใจ
- ตัวเลขใน dashboard, ตารางรายการ และการ์ดต่างๆ เป็นข้อมูลตัวอย่าง (mock) ทั้งหมด
