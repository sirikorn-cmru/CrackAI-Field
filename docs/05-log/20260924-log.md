# Log 2026-09-24

## `/sync-technical-spec` — ชั้นที่ 3 (detailed-design) — ปิดช่องว่าง NFR-03 "รองรับบางส่วน" ที่ `nfr-reviewer` รอบที่ 11 รายงาน

**Agent**: `detailed-design-writer`
**บริบท**: ต่อจากรอบที่ `api-spec.md` §14.2/§16 และ `db-spec.md` §10 ข้อ 3/§11 ถูกแก้ไว้แล้วว่า Phase 1 ไม่มีการเปรียบเทียบค่าระดับฟิลด์เลย — กรณีสองอุปกรณ์แก้คนละฟิลด์ของ `SurveyedBuilding` เดียวกัน (ซึ่งทฤษฎี merge อัตโนมัติได้) ก็ยังถูกส่งให้มนุษย์แก้มือเสมอ เพราะไม่มีกลไกแยกแยะออกจากความขัดแย้งจริง — ผู้ใช้ยืนยันรับสถานะนี้ใน Phase 1 แล้ว (2026-09-04)

### สถานะก่อนแก้
- `detailed-design/offline-sync.md` §4 ย่อหน้าสุดท้าย (เรื่อง auto-merge) มีถ้อยคำ**ขัดกับการตัดสินใจใหม่**: ระบุว่า "merge อัตโนมัติสำเร็จ" หมายถึงกรณี "แก้ไขคนละฟิลด์ หรือค่าตรงกันพอดี" ซึ่งฟังดูเหมือนสองกรณีนี้ถูก merge อัตโนมัติได้จริง — ขัดกับ `api-spec.md` §14.2/`db-spec.md` §10 ข้อ 3 ฉบับล่าสุดที่ระบุชัดว่าทั้งสองกรณีนี้ก็ถูกส่งให้มนุษย์เช่นกัน (เพราะนับเป็นการชนกันของเวอร์ชันระดับ aggregate)
- `detailed-design/manual-conflict-resolution.md` §5 ตรวจแล้ว **ไม่มีถ้อยคำขัดแย้ง** ไม่ต้องแก้

### การแก้ไข
- แก้ `docs/02-design/02-technical/detailed-design/offline-sync.md` §4 ย่อหน้าสุดท้าย (auto-merge):
  - เปลี่ยนนิยาม "merge อัตโนมัติสำเร็จ" ให้หมายถึง**เฉพาะกรณีเวอร์ชันระดับ aggregate ไม่ชนกันเลย (fast-forward)** เท่านั้น
  - เพิ่มถ้อยคำชัดเจนว่า เมื่อเวอร์ชันระดับ aggregate ชนกัน **ทุกกรณีต้องส่งมนุษย์เสมอ รวมถึงกรณีแก้คนละฟิลด์/ค่าตรงกันบังเอิญ**
  - เพิ่มบันทึกว่า **NFR-03 รองรับบางส่วน**ในเฟสนี้ พร้อมอ้างอิง [[db-spec#10. กฎทางธุรกิจที่กระทบโครงสร้างข้อมูล|db-spec หัวข้อ 10 ข้อ 3]] และ [[api-spec#16. ประเด็นรอตัดสินใจ|api-spec หัวข้อ 16]]
  - ไม่แตะ sequence diagram (§1), operation↔entity table (§2), state diagram (§3), edge case table (§5) — ตามข้อห้าม

### ไม่ได้แก้
- `manual-conflict-resolution.md` — ตรวจแล้วถ้อยคำสอดคล้องกับการตัดสินใจใหม่อยู่แล้ว ไม่จำเป็นต้องแก้

### คำถามที่ต้องถามผู้ใช้
- ไม่มี (ผู้ใช้ตัดสินใจไว้แล้วตามบริบทงาน)

### ช่องว่างของ api-spec/db-spec ที่พบระหว่างทาง
- ไม่พบเพิ่มเติม — งานรอบนี้เป็นการ sync ถ้อยคำใน detailed-design ให้ตรงกับชั้นบนที่ปิดไปแล้วเท่านั้น

### ไฟล์ที่แก้ไข
- `docs/02-design/02-technical/detailed-design/offline-sync.md`
