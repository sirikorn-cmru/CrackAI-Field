# Log 2026-09-03

## `/sync-phase-plan` — จัด FR-32/FR-33 เข้าแผน phase (ผ่าน subagent `phase-planner`)

### บริบท

`/audit-pipeline` (ก่อนหน้านี้) ตรวจพบว่า NFR-05 กำหนดให้เก็บประวัติ "เพื่อการตรวจสอบย้อนหลัง" แต่ทั้งระบบไม่มีช่องทางอ่านเลย ผู้ใช้จึงเพิ่ม FR-32 (เรียกดูประวัติการแก้ไขผลประเมินรายอาคาร) และ FR-33 (ค้นหา/กรองประวัติทั่วทั้งระบบ) เป็น FR ใหม่ ระดับความสำคัญกลางทั้งคู่ เอกสารทุกชั้น (AC/TC, architecture, api-spec, db-spec, detailed-design, prototype) sync ครบแล้วก่อนหน้านี้ เหลือเพียงแผน phase/task ที่ยังไม่ได้จัด — `backlog.md` มี 43 รหัส (FR-01–FR-33, NFR-01–NFR-10) แต่ `release-plan.md`/`03-task/` เดิม (เขียนไว้ 2026-09-01) ครอบคลุมเพียง 41 รหัส

### แผนที่เสนอ (subagent วิเคราะห์)

เสนอให้ใส่ FR-32 และ FR-33 เข้า **Phase 4 เดิม** (ไม่สร้าง Phase 5 ใหม่) โดยให้เหตุผลหลัก:

1. Dependency ของ FR-32 (เหตุการณ์จาก FR-14/Phase 3, FR-18-19/Phase 2, FR-31/Phase 4, sync-conflict/Phase 1) ครบสมบูรณ์ที่สุดที่ปลาย Phase 4 เพราะ FR-31 (ชิ้นสุดท้ายของห่วงโซ่) อยู่ Phase 4 อยู่แล้ว
2. สิทธิ์การเข้าถึงของ FR-32/33 (หัวหน้าผู้สำรวจ/ผู้ดูแลระบบ/หน่วยงานส่วนกลาง) ตรงกับกลุ่มบทบาท back-office เดียวกับรหัสอื่นใน Phase 4 ทั้งหมด
3. FR-33 มีลักษณะเป็นเครื่องมือค้นหา/กรองแบบเดียวกับ FR-24 ที่มีอยู่แล้วใน Phase 4 เพียงเปลี่ยนขอบเขตข้อมูล

ทางเลือกที่พิจารณาแล้วไม่เลือก:
- **สร้าง Phase 5 ใหม่** — ปฏิเสธเพราะ Phase 5 จะต้องรอทั้ง Phase 3 และ Phase 4 มาบรรจบ ทำลายคุณสมบัติ "Phase 3/4 ทำขนานกันได้" ที่แผนเดิมระบุไว้ชัด กลายเป็นเพิ่มคอขวดเพื่อ 2 รหัสระดับกลางเท่านั้น เกินความจำเป็น
- **แยก FR-32 เข้า Phase 4 แล้วเลื่อน FR-33 ออก** — ไม่ได้เลือก (ผู้ใช้ยืนยันแนวทาง "Phase 4 ทั้งคู่" ตามที่เสนอ)

ระบุหมายเหตุเชิงลำดับไว้ในเอกสารด้วยว่า: หน้าประวัติของ FR-32 จะแสดงเหตุการณ์แก้ไขผล AI ได้ครบก็ต่อเมื่อ Phase 3 ทำเสร็จแล้วเช่นกัน จึงสนับสนุนคำแนะนำเดิมของแผนว่าควรทำ Phase 3 ก่อน Phase 4 เมื่อทรัพยากรจำกัด (ไม่ใช่ hard build gate — ไม่แก้ผัง Mermaid)

### การยืนยันจากผู้ใช้

ผู้ใช้ยืนยันผ่าน `AskUserQuestion` ในเทรดหลัก (subagent เรียกใช้ `AskUserQuestion` ไม่ได้ในเซสชันนี้ จึงส่งแผนกลับให้ coordinator/เทรดหลักถามแทน) เลือกตัวเลือก **"Phase 4 ทั้งคู่"** ตามที่เสนอทุกประการ

### ไฟล์ที่แก้ไข/สร้าง

- `docs/01-requirements/02-plan/release-plan.md` (แก้ไข) — เพิ่มบรรทัดอัปเดต 2026-09-03, ปรับภาพรวม Phase 4 (6→8 รหัส), ปรับสรุปจำนวนรวม (41→43 รหัส, FR-01–FR-31→FR-01–FR-33), ปรับตารางสรุป Phase↔FR/NFR แถว Phase 4 (เพิ่ม FR-32/FR-33 + เหตุผล + หมายเหตุลำดับ Phase 3 ก่อน Phase 4), ปรับหัวข้อ/รหัสที่ครอบคลุม/exit criteria ของ Phase 4 (เพิ่ม 2 bullet สำหรับ FR-32/FR-33) — ผัง Mermaid ไม่แก้ไข (soft/data dependency ไม่ใช่ hard build gate)
- `docs/01-requirements/03-task/phase-4-backoffice-management-tasks.md` (แก้ไข) — เพิ่ม T-4-07 (FR-32) และ T-4-08 (FR-33), ปรับบรรทัดสรุปท้ายไฟล์ (6→8 tasks, เพิ่มรหัส), ปรับหัวข้อ/บรรทัดอธิบาย intro ให้ระบุ dependency ข้าม phase เพิ่มเติม (T-2-01, T-2-16, T-2-17, T-2-18, T-3-02)
- `docs/05-log/20260903-log.md` (สร้างใหม่) — ไฟล์นี้

### ผลลัพธ์

- Phase 1: 11 รหัส (ไม่เปลี่ยน) · Phase 2: 20 รหัส/19 tasks (ไม่เปลี่ยน) · Phase 3: 4 รหัส/4 tasks (ไม่เปลี่ยน) · Phase 4: 8 รหัส/8 tasks (เดิม 6/6 + FR-32, FR-33)
- รวม **43/43 รหัส** ครบทุกรายการใน [[backlog]] ไม่มีรหัสตกหล่น
- ไม่มีการอ้างอิง tech stack ใดๆ ในเนื้อหาที่เพิ่ม (เขียนเชิงพฤติกรรม/ผลลัพธ์ล้วน)
- ไม่ได้แตะ `backlog.md`, `feature-list.md`, `user-journey.md`, `01-spec/`, `02-design/`, `03-testing/` หรือ log ของวันก่อนหน้า

## `/build-prototype` (ขอบเขตแคบ) — แก้ 4 ปุ่มไร้ handler ที่ผู้ใช้พบจากการทดสอบเบราว์เซอร์จริง (ผ่าน subagent `prototype-writer`)

### บริบท

ผู้ใช้เปิด prototype เวอร์ชัน `20260830-01-v1` ในเบราว์เซอร์จริงและกดปุ่มครบทั้ง 19 หน้าจอ (เรียก handler จริง 77 ตัว ไม่มี error) พบปุ่ม 4 จุดที่มีหน้าตากดได้แต่ไม่มีทั้ง `onclick` และ `disabled` — กดแล้วไม่เกิดอะไรเลย โดยเฉพาะปุ่มบรรทัด 208 ของ `field-survey-photo-ai.html` (`📷 ถ่ายภาพใหม่` ในสถานะ AI วิเคราะห์ไม่สำเร็จ) เป็นเส้นทางที่ FR-28 กำหนดโดยตรง และปุ่มบรรทัด 152 (`📍 ลองจับพิกัดอีกครั้ง` ในสถานะ GPS ไม่แม่นยำ) เป็นเส้นทางกู้คืนของ NFR-06 — ขอบเขตงานนี้จำกัดเฉพาะ 4 ปุ่มนี้ในไฟล์ `field-survey-building.html` และ `field-survey-photo-ai.html` เท่านั้น (ผู้ใช้ระบุโฟลเดอร์เป้าหมาย `20260830-01-v1` และไม่ต้องถามเรื่องเวอร์ชันใหม่/เดิม)

### การแก้ไข (เรียงตามไฟล์/บรรทัดเดิม)

| ไฟล์ | บรรทัดเดิม | ปุ่ม | วิธีแก้ | เหตุผล |
|---|---|---|---|---|
| `field-survey-building.html` | 90 | `📍 จับพิกัดปัจจุบันอีกครั้ง` (สถานะ GPS ปกติ) | `alert('จับพิกัดปัจจุบันใหม่แล้ว (สาธิต) — ความแม่นยำ: ±4 เมตร ผ่านเกณฑ์ NFR-06 ระบบบันทึกพิกัดล่าสุดทับค่าเดิม')` | ไม่มีสถานะอื่นในไฟล์ที่แทนผล "จับพิกัดสำเร็จอีกครั้งจากสถานะที่สำเร็จอยู่แล้ว" ได้ — ใช้ alert ตามธรรมเนียมเดิมของไฟล์ ระบุค่าความแม่นยำจริงแทนคำว่า "สาธิต" ลอยๆ |
| `field-survey-building.html` | 152 | `📍 ลองจับพิกัดอีกครั้ง` (สถานะ GPS ไม่แม่นยำ ใน `#demo-gps`) | `onclick="showBuildingDemo('gps')"` | ไฟล์มีฟังก์ชัน `showBuildingDemo()` อยู่แล้วซึ่ง toggle: เรียกซ้ำกับ id ที่กำลัง active จะปิด panel นั้นกลับสู่สถานะปกติ (การ์ด GPS ด้านบนที่แสดง ±4 เมตร ผ่านเกณฑ์อยู่แล้ว) — ตรงกับที่ผู้ใช้ระบุ "พากลับไปสถานะ GPS ปกติ" พอดี โดยไม่ต้องเพิ่มโค้ด/สถานะใหม่ |
| `field-survey-photo-ai.html` | 59 | `📷 ถ่ายภาพ` (ก่อนเริ่มเส้นทาง AI/กรอกเอง) | `alert('ถ่ายภาพแล้ว (สาธิต) — ภาพถูกบันทึกลงเครื่องและผูกกับตำแหน่ง/บริเวณที่เลือกไว้ด้านบน (FR-12)')` | ไม่มีสถานะแยกใน `showPath()` ที่แทน "ถ่ายภาพครั้งแรกเสร็จ" ก่อนเข้าสู่ทางเลือก AI/กรอกเอง (การ์ดถัดไปเป็นจุดตัดสินใจ ไม่ใช่ผลของการถ่ายภาพ) — ใช้ alert ตามธรรมเนียมเดิม อ้างอิง FR-12 |
| `field-survey-photo-ai.html` | 208 | `📷 ถ่ายภาพใหม่` (สถานะ AI วิเคราะห์ไม่สำเร็จ ใน `#panel-ai-fail`) | `onclick="showPath('ai-pending')"` | ไฟล์มีฟังก์ชัน `showPath()` และ panel `ai-pending` อยู่แล้ว (จุดเดียวกับตอนเริ่มเลือก "ให้ AI ช่วยวิเคราะห์ก่อน") ใช้แทนผล "ถ่ายภาพใหม่แล้วส่งเข้าสู่การวิเคราะห์รอบใหม่" ได้ตรงความหมายที่สุดโดยไม่ต้องสร้างสถานะใหม่ — เป็นเส้นทางกู้คืนที่ FR-28 กำหนดโดยตรง |

### การตรวจสอบตัวเอง (กันบั๊กที่เคยเกิดซ้ำ 2 ครั้ง — ผูก `onclick` ปุ่มแรกแล้วลืมปุ่มที่เหลือ)

ไล่ grep `<button` ทั้งสองไฟล์หลังแก้ไขครบ ยืนยันว่าทุก `<button>` มีทั้ง `onclick` หรือ `disabled` แล้ว — ไม่มีปุ่มไร้ handler เหลืออยู่ (ปุ่ม `disabled` ที่บรรทัด 118 ของ `field-survey-photo-ai.html` คงไว้ตามเดิมตามที่ผู้ใช้ระบุว่าถูกต้องแล้ว ไม่ได้แตะ)

### ไฟล์ที่แก้ไข

- `docs/02-design/01-prototypes/20260830-01-v1/field-survey-building.html` (แก้ไข) — 2 จุด: บรรทัด 90, 152
- `docs/02-design/01-prototypes/20260830-01-v1/field-survey-photo-ai.html` (แก้ไข) — 2 จุด: บรรทัด 59, 208
- `docs/05-log/20260903-log.md` (แก้ไข — append) — ส่วนนี้

### สิ่งที่ไม่ได้แตะ

ไม่ได้แตะ `style.css`, `index.html`, `prototype.md`, ไฟล์ HTML อื่นในโฟลเดอร์ `20260830-01-v1`, โฟลเดอร์ prototype เวอร์ชันอื่น, หรือเอกสารชั้นใดใน `01-requirements/`/`02-design/`/`03-testing/` — ไม่มีคำถามเพิ่มเติมต่อผู้ใช้ระหว่างทาง (ขอบเขตและวิธีแก้ชัดเจนจาก prompt ทั้งหมดแล้ว) และไม่พบปัญหาเชิงโครงสร้างอื่นนอกเหนือ 4 จุดที่ระบุมา

## `/audit-prototype` (ขอบเขต: ทั้งระบบ) — ตรวจ prototype `20260830-01-v1` เทียบเอกสารทุกชั้น เน้นทิศทางย้อนขึ้นตามบริบท git ที่ผู้เรียกใช้ให้มา (ตรวจอย่างเดียว, subagent `prototype-auditor`)

### ข้อจำกัดของการตรวจ (ต้องระบุเสมอ)
การตรวจทั้งหมดทำโดยอ่านไฟล์เท่านั้น (`prototype.md`, ไฟล์ HTML ที่เกี่ยวข้อง, `style.css`, `DESIGN.md`, `feature-list.md`, `user-journey.md`, `acceptance-criteria.md`, `test-cases/audit-trail-*.md`, `db-spec.md`, `api-spec.md`) **ไม่ได้เปิดดูด้วยเบราว์เซอร์จริง** ปัญหาเชิงการแสดงผลจริง (layout, สีจริงบนจอ, การทำงานของ `onclick`/JS ตอนคลิกจริง) ตรวจแบบนี้ไม่เจอ

### ขอบเขตและบริบทที่ได้รับ
ผู้เรียกใช้ระบุ (จากผล `git log`) ว่า prototype ถูกแก้ล่าสุด (2026-09-03 13:17, `b9e2a80`) หลังเอกสาร `feature-list.md`/`backlog.md` (2026-09-02 11:57) และ `DESIGN.md` (2026-09-03 07:24) — ให้เน้นทิศทางย้อนขึ้นเป็นหลักแต่ห้ามข้ามทิศทางไล่ลง ไฟล์ที่ถูกแก้ใน 6 commit ล่าสุดที่ต้องเน้นอ่าน: `audit-trail-building.html`/`audit-trail-search.html` (ไฟล์ใหม่), `index.html`, `admin-users.html`, `style.css`, `lead-review-detail.html`, `building-search.html`, `prototype.md` (4 จุดที่แก้ใน `field-survey-building.html`/`field-survey-photo-ai.html` ผู้เรียกใช้ยืนยันแล้วว่าทดสอบเบราว์เซอร์จริงผ่าน ไม่ตรวจซ้ำ)

### หาเวอร์ชัน prototype
Glob `docs/02-design/01-prototypes/*` พบโฟลเดอร์เดียว `20260830-01-v1` (19 ไฟล์ HTML + `style.css` + `prototype.md`) — ตรวจเวอร์ชันนี้เวอร์ชันเดียว

### ตรวจไฟล์ ↔ ตาราง prototype.md (เบาะแสสำคัญที่สุดของทิศทางย้อนขึ้น)
Glob ไฟล์ `*.html` จริง 19 ไฟล์ เทียบกับตารางใน `prototype.md` — **ตรงกันครบทุกไฟล์ 19/19 ไม่มีไฟล์กำพร้า ไม่มีไฟล์ในตารางที่หายไปจริง** รวมทั้ง `audit-trail-building.html`/`audit-trail-search.html` ที่เพิ่มเข้ามาใหม่ก็มีแถวในตารางครบ ไม่มีสัญญาณว่ามีคนแก้ prototype โดยตรงโดยไม่อัปเดต `prototype.md`

### ทิศทางย้อนขึ้น (prototype → เอกสาร) — เน้นตามที่ผู้เรียกใช้ระบุ

**1) 2 หน้าจอใหม่ + drill-down 3 จุด เทียบเอกสารทุกชั้น:**
- `feature-list.md` มีฟีเจอร์ 16 (FR-32)/17 (FR-33) ตรงกับ `audit-trail-building.html`/`audit-trail-search.html` ทุกประการ (บทบาท, คำอธิบาย, wikilink กลับ spec หัวข้อ 4.8) — ไม่มีส่วนต่าง
- `user-journey.md` มี decision node + ขั้นตอนของ FR-32/FR-33 ครบใน 3 journey ที่เกี่ยวข้อง (หัวหน้าผู้สำรวจ, ผู้ดูแลระบบ, หน่วยงานส่วนกลาง/ผู้บริหาร) ตรงกับ 5 สถานการณ์ใน `audit-trail-building.html` และ 4 สถานการณ์ใน `audit-trail-search.html` ทุกจุด — ไม่มีส่วนต่าง
- `acceptance-criteria.md` (FR-32 AC-1..5, FR-33 AC-1..4) และ `test-cases/audit-trail-building.md` (TC-16-01..05)/`test-cases/audit-trail-search.md` (TC-17-01..04) — ไล่ตรวจทีละข้อเทียบกับ scenario จริงใน HTML ครบ **9/9 AC-set มีหน้าจอ/สถานะรองรับ** (happy path, empty state, ปฏิเสธสิทธิ์ทุกบทบาทที่ต้องปฏิเสธ, no-results state) ไม่มี negative case ใดตกหล่น
- `db-spec.md` §7.2 `AuditTrailEntry` — ตัวเลือก dropdown "ประเภทการกระทำ" ใน `audit-trail-search.html` (11 ค่า) ตรงกับ enum `action_type` ทุกค่า ไม่ขาดไม่เกิน ฟิลด์ที่แสดงในตาราง (เวลา/ผู้กระทำ/ประเภทการกระทำ/ค่าเดิม/ค่าใหม่/อาคาร) ตรงกับ attribute ที่มีจริงทุกฟิลด์ ไม่มีคอลัมน์ที่ไม่มี attribute รองรับ
- `api-spec.md` §15.5.1/15.5.2 — ตัวกรอง 4 ตัวใน `audit-trail-search.html` (ช่วงเวลา/ผู้แก้ไข/ประเภทการกระทำ/พื้นที่) และกฎ "หัวหน้าผู้สำรวจไม่มีสิทธิ์ FR-33" ตรงกันเป๊ะ
- Drill-down 3 จุด (`lead-review-detail.html` ลิงก์ "ดูประวัติการแก้ไขของอาคารนี้", `building-search.html` คอลัมน์ "ประวัติการแก้ไข" ครบทุกแถว, `admin-users.html` ปุ่มระดับหน้า + ลิงก์ "ดูประวัติ" ครบ 4 แถว) — ตรวจสิทธิ์ของบทบาทที่เข้าถึงแต่ละหน้าเดิมได้แล้ว พบว่าทุกบทบาทที่เห็นลิงก์มีสิทธิ์ FR-32/FR-33 อยู่แล้วตามเอกสาร ไม่มีการรั่วสิทธิ์
- **สรุป: ทิศทางย้อนขึ้นของ 2 หน้าจอใหม่ + drill-down — เอกสารทุกชั้นตามทันครบแล้วจริง ไม่มีพฤติกรรมส่วนเกินที่ไม่มีรหัสรองรับ**

**2) `prototype.md` (+38 บรรทัด):** ตรวจหัวข้อ "อัปเดต 20260902" ทั้งหมด เทียบกับไฟล์จริง — ตารางไฟล์↔ไฟล์จริงตรง 19/19, จำนวนฟีเจอร์ (17) และรหัส (FR-01–33, NFR-01–10 = 43) ตรงกับ `feature-list.md`/`backlog.md`, คำอธิบาย pattern ตัวตนผู้กระทำตรงกับ `style.css`/`DESIGN.md` §3.11 ทุกตัวอักษร — **สะท้อนสถานะจริงถูกต้อง ไม่มีส่วนต่าง**

**3) การใช้ `DESIGN.md` §3.11/UX-15 ใน 2 หน้าจอใหม่:**
- ไอคอนคน (วงกลม+เส้นไหล่ outline) กับไอคอนระบบ (เฟือง 8 แฉก outline) รูปทรงต่างกันชัดเจนโดยไม่พึ่งสี ตรง UX-02
- ทุกแถวที่ `action_type` = "ซิงค์ข้อมูลสำเร็จ"/"ตรวจพบความขัดแย้งของข้อมูล" แสดง "ระบบ" + อุปกรณ์ต้นทางเสมอ ไม่มีช่องว่างเปล่าเลย (grep ยืนยันไม่พบ `<td>` ผู้กระทำว่างเปล่าหรือ "ไม่ระบุ" ในทั้ง 2 ไฟล์) ตรง UX-15
- ตรวจ `style.css` บรรทัด 434-441 (คลาส `.actor*`) และ grep คำว่า `state-` ในทั้ง 2 ไฟล์ HTML — **ยืนยันไม่มีการอ้างอิง token กลุ่ม `--state-*` ใดๆ กับ pattern นี้เลย** ใช้เฉพาะ `--text-primary`/`--text-secondary`/`--text-tertiary`/`--weight-medium` ตรงตามข้อห้ามใน §3.11 เป๊ะ (ไม่ซ้ำรอยปัญหาหยิบยืม token ผิดโดเมนที่เคยเกิด 3 ครั้งก่อนหน้า)
- **สรุป: ใช้ §3.11/UX-15 ถูกต้องครบทุกจุดที่ตรวจได้จากโค้ด ไม่มีส่วนต่าง**

**4) `index.html`:** ระบุ "ขอบเขต: ครบ 17 ฟีเจอร์ (Must have 11 + Should have 6)" และมี screen-card ของ 19 หน้าจอครบ จัดกลุ่มตาม journey ถูกต้อง (รวม `audit-trail-building.html` ใต้ journey หัวหน้าผู้สำรวจ+ผู้ดูแลระบบ, `audit-trail-search.html` ใต้ journey ผู้ดูแลระบบ+หน่วยงานส่วนกลาง) — **ไม่มีส่วนต่าง**

### ทิศทางไล่ลง (เอกสาร → prototype) — ตรวจไม่ข้าม แม้เน้นทิศทางย้อนขึ้นเป็นหลัก
ตรวจซ้ำว่าทุกรหัส FR-01–33/NFR-01–10 (43 รหัส) ที่มีใน `backlog.md`/`feature-list.md` ยังปรากฏในตาราง `prototype.md` ครบ ไม่มีรหัสใดถูกถอดออกไปโดยไม่ตั้งใจ และไม่มีฟีเจอร์ใดใน `feature-list.md` ที่ไม่มีไฟล์ HTML รองรับเลย — **ไม่พบส่วนต่างทิศทางนี้ในรอบนี้**

### ส่วนต่างที่พบจริง (จุดเดียว) — เอกสารทดสอบมีหมายเหตุที่ล้าสมัยแล้ว
`docs/03-testing/01-test-plan/acceptance-criteria.md` (หัวข้อ 16/17, บรรทัด ~292, ~306) และ `docs/03-testing/01-test-plan/test-cases/audit-trail-building.md`/`audit-trail-search.md` (บรรทัด 5 ของแต่ละไฟล์) ยังคงมีข้อความหมายเหตุเดิมว่า **"ยังไม่มีหน้าจอ prototype รองรับฟีเจอร์นี้ ณ ขณะเขียนไฟล์นี้ (ตรวจแล้วว่าทั้ง 17 หน้าจอ...ไม่มีหน้าจอ audit trail ใดๆ)"** พร้อมท้ายข้อความระบุเองว่า "เมื่อมี prototype รองรับในรอบถัดไป ให้ทบทวน AC/Test Step เหล่านี้อีกครั้งเพื่อผูกชื่อหน้าจอจริง" — ปัจจุบัน prototype มีหน้าจอรองรับครบแล้วตั้งแต่ 2026-09-02 (ยืนยันจาก log วันนั้น: รอบตรวจซ้ายท้ายวันของ `prototype-auditor` ระบุ TC ครบ 9/9 แล้ว) แต่ไม่มีรอบใดกลับไปแก้ข้อความหมายเหตุนี้ใน AC/TC ให้ตรงความจริง — **นี่คือ cosmetic/metadata staleness ล้วน ไม่กระทบความครอบคลุมเชิงเนื้อหา** (เทียบ TC ทีละข้อกับ scenario จริงแล้วครบ 9/9 ตามที่ตรวจในหัวข้อ 1 ด้านบน) แต่ยังเป็นส่วนต่างจริงที่ควรแก้ให้ตรงกับความจริงปัจจุบัน

### สรุปสถานะ prototype
**Up to date เกือบสมบูรณ์** — ทิศทางย้อนขึ้นและทิศทางไล่ลงของ 2 หน้าจอใหม่ (FR-32/FR-33) sync กับเอกสารทุกชั้นครบ 100% ไม่มีพฤติกรรมส่วนเกิน ไม่มีการรั่วสิทธิ์ ไม่มีการหยิบยืม token ผิดโดเมน — ส่วนต่างเดียวที่พบคือหมายเหตุล้าสมัยใน `acceptance-criteria.md`/`test-cases/audit-trail-*.md` ที่ยังอ้างว่า "ยังไม่มี prototype" ทั้งที่มีแล้ว (metadata เท่านั้น ไม่กระทบตรรกะการทดสอบ)

### สิ่งที่ไม่ได้ตรวจในรอบนี้
- 17 หน้าจอเดิมที่ไม่ได้อยู่ในรายการไฟล์ที่ผู้เรียกใช้ระบุว่าถูกแก้ใน 6 commit ล่าสุด (อ้างอิงผลตรวจละเอียดจากรอบก่อนหน้าที่บันทึกไว้แล้วใน `prototype.md`/log วันที่ 20260831-20260902)
- 4 จุดที่แก้ใน `field-survey-building.html`/`field-survey-photo-ai.html` (บรรทัด 90/152/59/208) — ผู้เรียกใช้ยืนยันแล้วว่าทดสอบเบราว์เซอร์จริงผ่านทั้ง 4 จุด ไม่ตรวจซ้ำตามที่ระบุมา
- ไม่ได้ตรวจ AC/TC เดิมของฟีเจอร์ 1-15 ซ้ำ (นอกขอบเขตที่เน้นมารอบนี้ ยึดผลตรวจยืนยันจากรอบก่อนหน้า)
- ไม่ได้ตรวจ `nfr-review.md`, `architecture.md`, `detailed-design/*.md` อื่นนอกเหนือ audit-trail-building.md ซ้ำโดยละเอียด
- ไม่ได้เปิดดูด้วยเบราว์เซอร์จริงตามข้อจำกัดที่ระบุไว้ต้นหัวข้อ — โดยเฉพาะคอนทราสต์จริงของ `--text-secondary`/`--text-tertiary` บนพื้นตารางที่เรนเดอร์แล้ว และ layout จริงของ 2 หน้าจอใหม่

### สัญญาณที่ส่งต่อ
`NEEDS_TEST_PLAN_SYNC` เท่านั้น (ดูรายละเอียดด้านล่าง) — ไม่มี `NEEDS_PROTOTYPE_REBUILD`/`NEEDS_NEW_REQUIREMENT`/`NEEDS_FEATURE_JOURNEY_SYNC`/`NEEDS_DESIGN_SYSTEM` ในรอบนี้

---

## NEEDS_TEST_PLAN_SYNC

**อะไร**: หมายเหตุ/ข้อความใน `acceptance-criteria.md` (หัวข้อ 16, 17) และ `test-cases/audit-trail-building.md`/`test-cases/audit-trail-search.md` (บรรทัดหมายเหตุที่ 5 ของแต่ละไฟล์) ยังคงระบุว่า **"ยังไม่มีหน้าจอ prototype รองรับฟีเจอร์นี้ ณ ขณะเขียนไฟล์นี้"** ทั้งที่ปัจจุบัน prototype มีหน้าจอรองรับครบแล้ว (`audit-trail-building.html`, `audit-trail-search.html` ตั้งแต่ 2026-09-02) และท้ายข้อความเดิมระบุเองไว้แล้วว่า "เมื่อมี prototype รองรับในรอบถัดไป ให้ทบทวน AC/Test Step เหล่านี้อีกครั้งเพื่อผูกชื่อหน้าจอจริง"

**ที่ไหน**:
- `docs/03-testing/01-test-plan/acceptance-criteria.md` บรรทัดหมายเหตุก่อนหัวข้อ `#### FR-32` (~บรรทัด 292) และก่อนหัวข้อ `#### FR-33` (~บรรทัด 306)
- `docs/03-testing/01-test-plan/test-cases/audit-trail-building.md` บรรทัด 5
- `docs/03-testing/01-test-plan/test-cases/audit-trail-search.md` บรรทัด 5

**เพราะอะไร**: ข้อความหมายเหตุขัดกับความจริงปัจจุบันของ prototype (เปรียบเทียบแล้วว่า TC-16-01..05/TC-17-01..04 มีหน้าจอ/สถานการณ์ใน `audit-trail-building.html`/`audit-trail-search.html` รองรับครบ 9/9 จริง) หากปล่อยไว้จะทำให้ผู้ที่มาอ่านไฟล์นี้ในอนาคตเข้าใจผิดว่าไม่มี prototype รองรับ และคอลัมน์ Test Step ของทั้งสองไฟล์ test case ยังคงบรรยายเชิงพฤติกรรมล้วน ไม่ได้อ้างชื่อหน้าจอ/element จริง (เช่น `scenario-team`, `scenario-empty`, `role-lead`) ตามที่ผู้เขียนเดิมตั้งใจให้ทบทวนหลังมี prototype — **เป็นความคลาดเคลื่อนเชิง metadata/ความถูกต้องของเอกสารเท่านั้น ไม่ใช่ช่องว่างเชิงความครอบคลุมของการทดสอบ** (เนื้อหา AC/TC ที่มีอยู่ตรงกับพฤติกรรมจริงในหน้าจอครบทุกข้อแล้ว)

**ข้อเสนอให้ `test-writer` พิจารณา (ไม่ตัดสินใจแทน)**:
1. ลบ/แก้หมายเหตุ "ยังไม่มีหน้าจอ prototype รองรับ..." ในทั้ง 3 ไฟล์ให้ตรงกับความจริงปัจจุบัน
2. เติมชื่อหน้าจอ/element จริงในคอลัมน์ Test Step ของ `test-cases/audit-trail-building.md`/`audit-trail-search.md` (เช่น อ้างอิง `audit-trail-building.html` ปุ่มสาธิต `showScenario('team'/'empty'/'outside'/'admin'/'field')`, `audit-trail-search.html` ปุ่ม `showRole()`/`showResult()`) ตามรูปแบบเดียวกับไฟล์ test case อื่นที่มี prototype รองรับแล้ว

**อ้างอิง**: [[acceptance-criteria#16. เรียกดูประวัติการแก้ไขผลประเมินรายอาคาร (Audit Trail รายอาคาร)|acceptance-criteria ฟีเจอร์ 16]] · [[acceptance-criteria#17. ค้นหา/กรองประวัติการแก้ไขทั่วทั้งระบบ (Audit Trail ระดับหน่วยงาน)|ฟีเจอร์ 17]] · `test-cases/audit-trail-building.md` · `test-cases/audit-trail-search.md` · หน้าจอจริง `docs/02-design/01-prototypes/20260830-01-v1/audit-trail-building.html`/`audit-trail-search.html`

---

## `/sync-test-plan` (ขอบเขตแคบ: แก้เฉพาะหมายเหตุล้าสมัยของ FR-32/FR-33) — ปิดสัญญาณ `NEEDS_TEST_PLAN_SYNC` (ผ่าน subagent `test-writer`)

### บริบท
สืบเนื่องจาก `NEEDS_TEST_PLAN_SYNC` ด้านบน — prototype ของฟีเจอร์ 16 (FR-32)/17 (FR-33) มีหน้าจอรองรับแล้วตั้งแต่ 2026-09-02 และผู้ใช้ยืนยันทดสอบในเบราว์เซอร์จริงครบทุกสถานะแล้ว แต่หมายเหตุใน `acceptance-criteria.md`/`test-cases/audit-trail-*.md`/`test-plan.md` ยังระบุว่า "ยังไม่มีหน้าจอ prototype รองรับ" — งานนี้แก้เฉพาะหมายเหตุ/เติมชื่อหน้าจอจริงในคอลัมน์ขั้นตอน ไม่แก้เนื้อหา AC/test step ที่ถูกต้องอยู่แล้ว ไม่เพิ่ม/ลบ AC หรือ TC

### grep ยืนยันจุดที่ต้องแก้ (ทำเองอีกรอบ ไม่เชื่อรายการที่ผู้ใช้ให้มาอย่างเดียว)
grep คำว่า "ยังไม่มีหน้าจอ" / "ยังไม่มี prototype" / "ไม่มีหน้าจอ prototype" ทั้งโฟลเดอร์ `01-test-plan/` พบครบ **5 จุดใน 4 ไฟล์** ตรงกับที่ผู้ใช้ระบุ (รวมจุดที่ผู้ใช้บอกว่า "เจอเพิ่มเองที่ `test-plan.md` บรรทัด 34" ด้วย) — ไม่พบจุดตกค้างเพิ่มเติมนอกเหนือจากที่ระบุมา:
1. `acceptance-criteria.md` บรรทัด 292 (หัวข้อฟีเจอร์ 16/FR-32)
2. `acceptance-criteria.md` บรรทัด 306 (หัวข้อฟีเจอร์ 17/FR-33)
3. `test-cases/audit-trail-building.md` บรรทัด 5
4. `test-cases/audit-trail-search.md` บรรทัด 5
5. `test-plan.md` บรรทัด 34 (รายการ "นอกขอบเขต")

### การแก้ไข
- **`acceptance-criteria.md`** — แทนที่หมายเหตุล้าสมัยทั้ง 2 จุด (ก่อน `#### FR-32` และก่อน `#### FR-33`) ด้วยหมายเหตุอัปเดต 2026-09-03 ระบุชื่อไฟล์ prototype จริง (`audit-trail-building.html`, `audit-trail-search.html`) และสรุปว่าปุ่ม "มุมมองสาธิต" ครอบคลุมสถานการณ์ตรงกับ AC ทุกข้อ พร้อมยืนยันว่าผู้ใช้ทดสอบเบราว์เซอร์จริงผ่านแล้ว — ไม่แตะเนื้อหา AC-1..5/AC-1..4 เดิมเลย
- **`test-cases/audit-trail-building.md`** — แทนที่หมายเหตุบรรทัด 5 ด้วยหมายเหตุอัปเดต แล้วเติมชื่อหน้าจอ/องค์ประกอบจริงลงในคอลัมน์ขั้นตอนของทุกแถว (TC-16-01..05): อ้างอิง `audit-trail-building.html` + ปุ่ม "มุมมองสาธิต" ตัวเลือก 1-5 ตามสถานการณ์ของแต่ละ TC (`scenario-team`/`scenario-empty`/`scenario-outside`/`scenario-admin`/`scenario-field` ตามลำดับที่ตรงกับ TC) และเพิ่มจุด drill-down (`lead-review-detail.html`, `building-search.html`, `admin-users.html`) ในขั้นตอนที่เกี่ยวข้อง — ไม่แก้ Precondition/Expected Result/Test Data/AC ที่ทดสอบ/รหัส FR/ระดับความสำคัญ
- **`test-cases/audit-trail-search.md`** — เช่นเดียวกัน เติมชื่อหน้าจอ `audit-trail-search.html` + ปุ่ม "มุมมองสาธิต" ตัวเลือก 1-3 และชื่อ element ตัวกรองจริง (`f-from`, `f-to`, `f-actor`, `f-action`, `f-province`) และผลลัพธ์ (`result-list`, `result-empty`, `role-lead`, `role-field`) ลงในคอลัมน์ขั้นตอนของ TC-17-01..04 — ไม่แก้ Precondition/Expected Result/Test Data/AC ที่ทดสอบ/รหัส FR/ระดับความสำคัญ
- **`test-plan.md`** บรรทัด 34 — แก้ข้อความในรายการ "นอกขอบเขต" จาก "ยังไม่มี prototype รองรับ...รอ build-prototype สร้างหน้าจอในรอบถัดไป" เป็นข้อความยืนยันว่าหน้าจอมีแล้วตั้งแต่ 2026-09-02 และไม่ใช่รายการนอกขอบเขตอีกต่อไป (คงบรรทัดไว้เพื่อบันทึกประวัติการเปลี่ยนแปลง ไม่ได้ลบทิ้งทั้งบรรทัด)

### ตรวจซ้ำหลังแก้ (grep รอบสอง)
grep คำเดิมทั้งโฟลเดอร์อีกครั้งหลังแก้ไข — ไม่พบข้อความล้าสมัยเหลืออยู่เลย (match เดียวที่เจอคือข้อความใหม่ใน `test-plan.md` ที่อธิบายว่า "เดิมข้อนี้ระบุว่ายังไม่มี prototype รองรับ" ซึ่งเป็นส่วนหนึ่งของคำอธิบายที่แก้ไขแล้ว ไม่ใช่หมายเหตุล้าสมัยที่หลงเหลือ) — grep คำว่า `build-prototype`, `ไม่อ้างชื่อหน้าจอ`, `เชิงพฤติกรรม/ผลลัพธ์ที่สังเกตได้` ก็ไม่พบเหลือเช่นกัน

### ยืนยันยอดรวมไม่เปลี่ยน
- นับ `- AC-` ใน `acceptance-criteria.md` = **93** (ตรงตามที่ระบุ ไม่เพิ่ม/ลด)
- นับ heading `#### FR-xx`/`#### NFR-xx` = **43** (ตรงตามที่ระบุ ไม่เพิ่ม/ลด)
- ไม่ได้เพิ่ม/ลบแถว TC ใดใน `test-cases/audit-trail-building.md` (ยังคง 5 แถว TC-16-01..05) หรือ `test-cases/audit-trail-search.md` (ยังคง 4 แถว TC-17-01..04)

### ไฟล์ที่แก้ไข
- `docs/03-testing/01-test-plan/acceptance-criteria.md` — บรรทัด ~292, ~306 (หมายเหตุ 2 จุด)
- `docs/03-testing/01-test-plan/test-cases/audit-trail-building.md` — บรรทัด 5 (หมายเหตุ) + คอลัมน์ขั้นตอนของ TC-16-01..05 (5 แถว)
- `docs/03-testing/01-test-plan/test-cases/audit-trail-search.md` — บรรทัด 5 (หมายเหตุ) + คอลัมน์ขั้นตอนของ TC-17-01..04 (4 แถว)
- `docs/03-testing/01-test-plan/test-plan.md` — บรรทัด 34
- `docs/05-log/20260903-log.md` (แก้ไข — append) — ส่วนนี้

### สิ่งที่ไม่ได้แตะ / ยังไม่ครอบคลุม
- ไม่ได้แก้เนื้อหา AC หรือ test step อื่นใดนอกเหนือหมายเหตุ/การเติมชื่อหน้าจอตามที่ระบุ — ไม่มีการเพิ่ม/ลด AC หรือ TC
- ไม่ได้แตะ Precondition, Expected Result, Test Data, คอลัมน์ AC ที่ทดสอบ, รหัส FR/NFR, หรือระดับความสำคัญของแถวใดเลย
- ไม่ได้ทดสอบในเบราว์เซอร์จริง (ไม่มีเครื่องมือนี้) — อ้างอิงเฉพาะโครงสร้าง HTML/element ID ที่อ่านได้จากไฟล์ `audit-trail-building.html`/`audit-trail-search.html` เท่านั้น ยึดคำยืนยันของผู้ใช้ว่าทดสอบผ่านแล้วทุกสถานะ
- ไม่ได้ตรวจ/แก้ AC-TC ของฟีเจอร์ 1-15 หรือหัวข้ออื่นใน `test-plan.md` นอกเหนือบรรทัด 34 — อยู่นอกขอบเขตของงานนี้
- ไม่มี `NEEDS_NEW_REQUIREMENT` เกิดขึ้นในรอบนี้ (งานนี้เป็นการแก้ metadata ล้วน ไม่ได้คิด AC ใหม่)
